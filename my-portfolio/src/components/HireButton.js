import { Box } from '@mui/material'

const HireButton = () => {
    return (
        <Box sx={{
            width: 'fixed',
            height: '134px',
            background: '#FF0000',
        }}/>
    )
}

export default HireButton